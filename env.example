# AI4Bharat IndicConformer Configuration
# The system will automatically download and use IndicConformer models for Hindi/Punjabi

# Optional: Set to "cuda" for GPU acceleration, "cpu" for CPU only
DEVICE=auto
